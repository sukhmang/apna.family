import ProfilePage from './ProfilePage'

/**
 * MemorialProfile Page - Wrapper component
 * ProfilePage now uses context hooks directly, so no props needed
 */
export default function MemorialPage() {
  return <ProfilePage />
}
